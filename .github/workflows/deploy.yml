name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main  # или master, если у вас ветка называется иначе

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install and Build
        run: |
          npm install
          npm run build

      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: build # папка, которую создал npm run build
          branch: gh-pages
